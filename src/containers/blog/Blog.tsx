import React, { FunctionComponent } from 'react';

interface OwnProps {}

type Props = OwnProps;

const Blog: FunctionComponent<Props> = (props) => {

  return (<h1>Blog</h1>);
};

export default Blog;
